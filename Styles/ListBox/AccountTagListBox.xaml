<UserControl x:Class="UI4All.Styles.ListBox.AccountTagListBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI4All.Styles.ListBox"
             mc:Ignorable="d" 
             xmlns:templates="clr-namespace:UI4All.Styles.ListBox.Templates"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="AccountTemplate">
                <templates:AccountTemplate/>
            </DataTemplate>
            
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <ListBox x:Name="Main" Background="Transparent" BorderThickness="0">
            <ListBox.ItemTemplateSelector>
                <local:AccountTagContentTypeSelector AccountContentType="{StaticResource AccountTemplate}"/>
            </ListBox.ItemTemplateSelector>
        </ListBox>

    </Grid>
</UserControl>
